import 'package:flutter/material.dart';

class IntroductionMobilePage extends StatelessWidget {
  const IntroductionMobilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}