import 'package:flutter/material.dart';

import 'experience.dart';

class ExperienceWrapper extends StatelessWidget {
  const ExperienceWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const ExperiencePage();
  }
}