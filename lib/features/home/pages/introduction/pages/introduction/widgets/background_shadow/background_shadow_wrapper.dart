import 'package:flutter/material.dart';
import 'background_shadow_widget.dart';

class BackgroundShadowWrapper extends StatelessWidget {
  const BackgroundShadowWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const BackgroundShadowWidget();
  }
}
