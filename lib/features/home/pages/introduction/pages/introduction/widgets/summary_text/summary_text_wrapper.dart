import 'package:flutter/material.dart';
import 'summary_text_widget.dart';

class SummaryTextWrapper extends StatelessWidget {
  const SummaryTextWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const SummaryTextWidget();
  }
}
